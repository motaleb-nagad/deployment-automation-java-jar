# nginx serves the built SPA and reverse-proxies the API to the backend service.
# ${BACKEND_URL} is substituted from the container env at startup (nginx templates).
server {
    listen 8080;                 # unprivileged (non-root) port
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    server_tokens off;           # don't advertise the nginx version

    # Baseline security headers.
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "no-referrer" always;
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; connect-src 'self'" always;

    # API + SSE. proxy_buffering off is essential so deploy output streams live.
    location /api/ {
        proxy_pass ${BACKEND_URL};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }

    location /actuator/ {
        proxy_pass ${BACKEND_URL};
        proxy_set_header Host $host;
    }

    # SPA fallback — client-side routing.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
